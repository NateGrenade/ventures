---
description: Run a parallel sweep batch over the frontier (default 24 cells, 6 at a time)
---

Run a sweep batch using the niche-batch skill.

Batch size: $ARGUMENTS (if empty, use the skill's default of 24 cells)

Pull the cells from scripts/next_cells.py yourself — do not ask which cells to sweep.
