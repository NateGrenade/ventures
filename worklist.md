# Human-Executed Worklist

Tests `niche-falsify` could not run itself. Each entry arrives with the work pre-done:
who to contact, a call script, and — written down *before* the call — the answer that
would falsify the assumption.

The pre-registered falsifying answer is the point. Without it, every call outcome reads
as encouraging.

---

*(empty — populated by niche-falsify)*
